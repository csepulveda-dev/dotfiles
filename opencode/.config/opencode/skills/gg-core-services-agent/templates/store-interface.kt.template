package {{STORE_PACKAGE}}

import {{DOMAIN_PACKAGE}}.{{MODEL_NAME}}
import com.glossgenius.core.libs.models.*
import java.util.*

/**
 * Store interface for {{MODEL_NAME}} data access operations.
 */
interface {{MODEL_NAME}}Store {
    
    // Primary token-based methods
    fun findByToken(token: {{TOKEN_TYPE}}): {{MODEL_NAME}}?
    fun findByTokens(tokens: List<{{TOKEN_TYPE}}>): List<{{MODEL_NAME}}>
    fun findByBusinessToken(businessToken: BusinessToken): List<{{MODEL_NAME}}>
    
    {{#INCLUDE_GUID}}
    // Legacy GUID support methods
    fun findByGuid(guid: UUID): {{MODEL_NAME}}?
    fun findByGuids(guids: List<UUID>): List<{{MODEL_NAME}}>
    {{/INCLUDE_GUID}}
    
    // CRUD operations
    fun create({{MODEL_VAR}}: {{MODEL_NAME}}): {{MODEL_NAME}}
    fun update({{MODEL_VAR}}: {{MODEL_NAME}}): {{MODEL_NAME}}
    fun delete(token: {{TOKEN_TYPE}}): Boolean
    
    {{#SERVICE_SPECIFIC_METHODS}}
    {{SERVICE_SPECIFIC_METHODS}}
    {{/SERVICE_SPECIFIC_METHODS}}
    
    {{#CUSTOM_METHODS}}
    {{CUSTOM_METHODS}}
    {{/CUSTOM_METHODS}}
}