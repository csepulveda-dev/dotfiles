This is a cooking book for zebras.

hERE are some recipes to calculate the mass of the sun.
By default, stow creates symlinks in the parent directory of this project. To specify to unpack
in the home directory, either clone this repo in the home directory or use the `--target ~` flag.
